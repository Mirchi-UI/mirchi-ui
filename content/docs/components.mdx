---
title: Components
description: Browse our collection of beautifully designed, ready-to-use UI blocks to accelerate your development.
icon: Rocket
---

import Image from "next/image";
import { Badge } from "@/components/ui/badge";
import { navigationSections } from "@/config/navigation";
import Link from "next/link";

<div className="container mx-auto px-4 py-16">
  
  <div className="grid grid-cols-1 sm:grid-cols-2 md:grid-cols-3 gap-6">
    {navigationSections.find(sec => sec.title === "Components")?.items.map(item => (
      <Link
    href={item.href} 
    key={item.id}
    className="group relative rounded-2xl overflow-hidden shadow-xl border border-white/10
               transition-all duration-300 hover:scale-[1.02] hover:shadow-[0_20px_60px_-15px_rgba(0,0,0,0.5)]"
  >
        {/* Image Preview */}
        <div className="relative -top-12 w-full h-56  md:h-48 overflow-hidden rounded-t-2xl">
          {item.previewImage && (
            <Image
              src={item.previewImage}
              alt={`${item.title} preview`}
              fill
              className="object-cover"
            />
          )}
          
        </div>

        {/* Content */}
        <div className="px-4 flex flex-col  ">
          <div className="flex items-center justify-between">
            <h3 className="text-lg font-semibold text-white">{item.title}</h3>
            <div className="flex ">
              {item.isNew && (
                <Badge className="h-5 px-2 text-xs bg-blue-500 text-white border-0">
                  New
                </Badge>
              )}
              {item.isUpdated && (
                <Badge className="h-5 px-2 text-xs bg-green-500 text-white border-0">
                  Updated
                </Badge>
              )}
            </div>
          </div>
          <p className="text-sm text-zinc-400">{item.description}</p>
          <p className="text-xs text-zinc-500 mt-1">{item.count} Components</p>
        </div>
      </Link>
    ))}
  </div>
</div>
