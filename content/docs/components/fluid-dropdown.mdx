---
title: Fluid Dropdown
description: A dropdown component that adjusts its width based on the content size.
slug: /docs/components/fluid-dropdown
icon:  ArrowDownCircle
full: true

---

import { FluidDropdown } from "@/components/modules/drop-down/Fluid-dropdown.tsx";

## Fluid Dropdown Component

<Preview 
  link="drop-down/Fluid-dropdown.tsx"
  comment={["tailwindcss","dropdown","fluid","responsive"]}
>
  <FluidDropdown/>
</Preview>

 ---
# useClickAway Hook


```tsx
import { useEffect, type RefObject } from "react";

export function useClickAway(
  ref: RefObject<HTMLElement>,
  handler: (event: MouseEvent | TouchEvent) => void
) {
  useEffect(() => {
    const listener = (event: MouseEvent | TouchEvent) => {
      if (!ref.current || ref.current.contains(event.target as Node)) {
        return;
      }
      handler(event);
    };

    document.addEventListener("mousedown", listener);
    document.addEventListener("touchstart", listener);

    return () => {
      document.removeEventListener("mousedown", listener);
      document.removeEventListener("touchstart", listener);
    };
  }, [ref, handler]);
}


```




