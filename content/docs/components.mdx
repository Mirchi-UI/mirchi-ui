---
title: Components {hide}
description: Browse our collection of beautifully designed, ready-to-use UI blocks to accelerate your development.
icon: Rocket
---

import Image from "next/image";
import { navigationSections } from "@/config/navigation";
import Link from "next/link";

export const ComponentsGrid = () => {
  const items =
    navigationSections.find((sec) => sec.title === "Components")?.items ?? [];

  return (
    <div className="w-full">
      {/* Hero */}
      <div className="py-16">
        <div className="inline-flex items-center gap-2 px-3 py-1 text-[11px] font-semibold tracking-widest uppercase rounded-full border border-violet-500/30 bg-violet-500/10 text-violet-500 dark:text-violet-400 mb-6">
          <span className="w-1.5 h-1.5 rounded-full bg-violet-500 dark:bg-violet-400" />
          UI Component Library
        </div>

        <h1 className="   font-extrabold tracking-tight text-neutral-900 dark:text-neutral-100 leading-tight mb-5">
          Beautiful UI components built with <br />
          Tailwind CSS and{" "}
          <span className="text-violet-500 dark:text-violet-400">
            shadcn/ui
          </span>
        </h1>

        <p className="text-base text-neutral-600 dark:text-neutral-400 max-w-xl leading-relaxed mb-2">
          A collection of copy-and-paste components for quickly building
          application UIs. Free, open-source, and ready for your Next.js apps.
        </p>

       
      </div>

      {/* Grid */}
      <div className="grid grid-cols-2 sm:grid-cols-3 md:grid-cols-4 gap-6 md:gap-8">
        {items.map((item) => (
          <Link
            href={item.href}
            key={item.id}
            className="group block no-underline"
          >
            {/* Card */}
            <div className="relative w-full aspect-[5/4] rounded-2xl border border-neutral-200 dark:border-neutral-800 bg-white/70 dark:bg-neutral-900/60 backdrop-blur-xl overflow-hidden transition-all duration-300 hover:shadow-xl hover:-translate-y-1 hover:border-violet-500/40">

              {/* Image */}
              {item.previewImage ? (
                <Image
                  src={item.previewImage}
                  alt={item.title}
                  fill
                  className="object-contain p-6 group-hover:scale-105 transition-transform duration-500"
                />
              ) : (
                <div className="flex items-center justify-center w-full h-full text-neutral-400 dark:text-neutral-700">
                  <item.icon className="w-14 h-14  text-violet-500 dark:text-violet-400 group-hover:opacity-60 transition" />
                </div>
              )}

              {/* Subtle gradient hover */}
              <div className="absolute inset-0 opacity-0 group-hover:opacity-100 transition bg-gradient-to-t from-violet-500/10 to-transparent" />
            </div>

            {/* Text */}
            <div className="mt-5 text-center">
              <p className="text-base font-semibold text-neutral-900 dark:text-neutral-100 group-hover:text-violet-500 transition">
                {item.title}
              </p>

              {item.count && (
                <p className="text-xs text-neutral-500 mt-1">
                  {item.count} components
                </p>
              )}
            </div>
          </Link>
        ))}
      </div>
    </div>
  );
};

export default ComponentsGrid;
